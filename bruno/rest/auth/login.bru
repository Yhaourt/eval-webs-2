meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{REST_URL}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "testuser1@example.com",
    "password": "password"
  }
}
