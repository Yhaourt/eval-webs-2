meta {
  name: list (eval)
  type: graphql
  seq: 1
}

post {
  url: {{GRAPHQL_URL}}
  body: graphql
  auth: none
}

body:graphql {
  query {
    listRooms(skip: 2, limit: 10) {
      id
      name
      capacity
      location
      created_at
      reservations {
        id
        room_id
        user_id
        start_time
        end_time
        status
        created_at
      }
    }
  }
}
